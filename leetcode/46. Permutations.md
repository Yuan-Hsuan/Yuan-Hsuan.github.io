---
id: lc-permutations
domain: leetcode
title: 46. Permutations
tags: [backtracking]
difficulty: medium
mastery: 0
source: https://leetcode.com/problems/permutations/
visibility: public
---

## 🚀 Pitch

### The Strategy: Backtracking with a Bitmask
The problem requires us to generate all possible permutations of a given array. To do this cleanly and systematically, we can model it as a **decision tree** using **Backtracking**. 

### The Precise Execution

At each depth of our recursion, we choose a number that hasn't been used yet to append to our current path. To track visited elements with maximum efficiency, we can use an integer as a **Bitmask** (which is clean since constraints are small, e.g., $N \le 6$).
So the base case is when the bitmask is filled with ones — that means we've used all the numbers, so we record the current path.

Otherwise, we run the **pick-up a number → explore → un-pick-up** loop.

**Complexity**
- **Time — O(N × N!):** there are `N!`(n factorial) leaves (complete permutations); each one costs an extra O(N) to
  copy the finished path into the result.
> Finding permutations is a well-studied problem in combinatorics. Given a set of length n, the number of permutations is n! (n factorial). There are n options for the first number, n−1 for the second, and so on. For each of the n! permutations, we need O(n) work to copy curr into the answer. This gives us O(n⋅n!) work.
- **Space — O(N):** recursion depth is `n` plus the O(N) `curr` path; the bitmask is O(1).

## 🛠️ Optimization (最佳化程式碼)
```cpp
class Solution {
private:
    void backtracking(vector<int>& nums, int mask, vector<int>& curr, vector<vector<int>>& res) {

        if (curr.size() == nums.size()) {        
            res.push_back(curr);
            return;
        }

        for (int i = 0; i < nums.size(); i++) {
            int num = nums[i];

            if ((mask >> i) & 1) {      
                continue;
            }

            curr.push_back(num);        
            backtracking(nums, mask | (1 << i), curr, res);                
            curr.pop_back();            
        }
    }
public:
    vector<vector<int>> permute(vector<int>& nums) {
        vector<int> curr;
        vector<vector<int>> res;
        backtracking(nums, 0, curr, res);

        return res;
    }
};
```

### 💡 教練的啟發式引導 (Heuristic Challenge)

你已經掌握了「決策樹 + bitmask 標記 used」這套 permutation 骨架：每一層挑一個沒用過的數，選 → explore → un-pick。

如果今天輸入變成**有重複數字**，例如 `[1, 1, 2]`（**LeetCode 47. Permutations II**），上面這套 code 會吐出重複的排列（兩個 `1` 交換位置被當成兩種）。
**你會怎麼在這棵決策樹上「剪枝」把重複去掉？先排序有沒有幫助？在同一層遇到「值和前一個一樣、但前一個還沒被用」的數字，該跳過還是保留？** (不需要寫完整程式碼，請直接跟我分享你的思考邏輯！)

→ 我的解法:[[lc-permutations-ii]]
